const express = require('express')
const app = express()
const port = 3000
let registeredApp = []

setInterval(() => {
  registeredApp.filter
}, 5000)

app.get('/*', async (req, res) => {
  res.redirect()
})

app.listen(port, () => {
  console.log(`Example app listening at http://localhost:${port}`)
})